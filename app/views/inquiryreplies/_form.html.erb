<%= form_with(model: inquiryreply, local: true) do |form| %>
  <% if inquiryreply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(inquiryreply.errors.count, "error") %> prohibited this inquiryreply from being saved:</h2>

      <ul>
      <% inquiryreply.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :reply %>
    <%= form.text_area :reply %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :inquirydetail_id %>
    <%= form.text_field :inquirydetail_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
