<%= form_with(model: potentialbuyer, local: true) do |form| %>
  <% if potentialbuyer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(potentialbuyer.errors.count, "error") %> prohibited this potentialbuyer from being saved:</h2>

      <ul>
      <% potentialbuyer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :house_id %>
    <%= form.text_field :house_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
